# {{PROJECT_NAME}}

{{DESCRIPTION}}

## Layout

- `ARCHITECTURE.md` — the system map. Read it before changing structure.
- `docs/` — design records, sorted by tier. See `docs/README.md`.

## Living-document rule

Any change that alters system structure must update `ARCHITECTURE.md` in the
same PR. A map that lags the territory is worse than no map, because it is
trusted.

---

_Scaffolded by JARVIS on {{DATE}}._
